#include <iostream>

int main() {
     /*
    int grade = 71;

    grade >= 75 ? std::cout << "You pass!" : std::cout << "You fail!";

    */

    // int num = 31;

    // num % 2 ? std::cout << "Num is even!" : std::cout << "Num is odd!";
    
     bool hungry = true;

    // hungry ? std::cout << "Hungry" : std::cout << "Full";

    std::cout << (hungry ? "You are hungry" : "You are full!");


    return 0;
}